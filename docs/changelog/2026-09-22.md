# 2026-09-22

## Space Typing final platform checkpoint

### Child checkpoint

Space Typing reached the post-Step-68 integration checkpoint on child `main`:

```text
sinhvienaiti/space-typing
cba924d14b47613439ae5ee1ccc2b7ffa19a21d2
```

Child push CI #123 passed before the parent pin was updated.

### Parent integration verification

The parent already contained the Space Typing submodule declaration, Portal registry entry, focused/all dev commands, static Play build support, nginx host, TLS host list and Platform CI child test/build jobs.

The parent submodule pointer was still pinned to an early Space Typing checkpoint, so this work updates it to the tested child checkpoint above.

### Shared-data routing fix

Space Typing consumes:

```text
/vocabulary/
/shared/typing-texts/
```

on its own internal origin:

```text
https://space.typing-game.local
```

The Space Typing nginx server blocks previously proxied/served only the child application. Both Dev and Play nginx configs now expose the parent shared vocabulary and typing-text directories on the Space origin before the catch-all app route.

This prevents vocabulary/typing-text loading from silently falling back when Space Typing is launched through the parent Portal.

### Regression contract

Added:

```text
scripts/validate-space-integration.mjs
pnpm validate:space-integration
```

The contract verifies:

- all existing game IDs, routes and internal origins remain unchanged;
- all expected submodule paths remain registered;
- Space Typing dev/build scripts exist;
- `dev.sh` and `play.sh` include Space Typing;
- both nginx modes still contain every game host;
- both nginx modes expose the shared learning-data paths required by Space Typing.

Platform CI runs this contract and syntax-checks the validator.

### Documentation

Updated the root README so Space Typing appears in:

- platform description;
- repository structure;
- public Portal route;
- internal origin list;
- focused development examples;
- lower-level command examples;
- accepted launcher aliases.

## Remaining validation

The parent PR must pass Platform CI after the new child pin. The child roadmap still has manual balance/playtest checkpoints pending where browser feel cannot be verified by CI alone.
